.DS_Store
node_modules/
package-lock.json

# Pipeline intermediates: fetch caches, TFRRS result dumps and multi-megabyte joins.
# Every one is regenerated by the scripts in tools/ — see tools/README.md for the order.
tools/.work/
# Written by python3 whenever one tools/ script imports another (parse_xc, mostly).
tools/__pycache__/
